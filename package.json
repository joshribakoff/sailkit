{
  "name": "bearing-dev",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @bearing-dev/compass -w @bearing-dev/lantern && npm run build -w @bearing-dev/teleport --if-present",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
